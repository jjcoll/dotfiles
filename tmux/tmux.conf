# --- Splits ---
unbind %
unbind '"'
bind "'" split-window -v -c "#{pane_current_path}"
bind '"' split-window -h -c "#{pane_current_path}"
bind c new-window -c "#{pane_current_path}"

# --- Navigation ---
bind h select-pane -L
bind j select-pane -D
bind k select-pane -U
bind l select-pane -R

# Move the current pane to a new position using Leader + Shift + HJKL
# -v / -h  forces the pane to join as vertical (stacked) or horizontal (side-by-side)
# -b (before) tells tmux to place moved pane to the left or above the target pane
# .{directory-of} targets the pane physically located in that direction within the current window
bind H move-pane -b -v -t '.{left-of}'
bind L move-pane -h -t '.{right-of}'
bind K move-pane -b -v -t '.{up-of}'
bind J move-pane -v -t '.{down-of}'
# Move pane to the far LEFT of the window
bind Y move-pane -b -h -t '.{left}' 
# Move pane to the far RIGHT of the window
bind O move-pane -h -t '.{right}'
# --- Resize ---
bind -r C-h resize-pane -L 5
bind -r C-j resize-pane -D 5
bind -r C-k resize-pane -U 5
bind -r C-l resize-pane -R 5

# --- Copy mode (vi keys) ---
set -g mode-keys vi
set -g status-keys vi

# Enter copy mode with Leader + Escape (default Leader + [ still works)
bind Escape copy-mode

# Visual-style selection and yank to macOS clipboard
bind -T copy-mode-vi v send-keys -X begin-selection
bind -T copy-mode-vi C-v send-keys -X rectangle-toggle
bind -T copy-mode-vi y send-keys -X copy-pipe "pbcopy"
bind -T copy-mode-vi Escape send-keys -X cancel

# Mouse drag selects and copies straight to clipboard (mouse already on)
bind -T copy-mode-vi MouseDragEnd1Pane send-keys -X copy-pipe-and-cancel "pbcopy"

# --- Cursor & Term ---
set -ga terminal-overrides '*:Ss=\E[%p1%d q:Se=\E[2 q'
set -g mouse on

# --- Windows ---
set -g renumber-windows on    # Renumber when a window is closed

# --- Reload ---
# Note: Ensure the path matches exactly where your file is
bind r source-file ~/.config/tmux/tmux.conf \; display-message "Config reloaded!"

# --- Pane borders ---
set -g pane-border-style fg=colour238
set -g pane-active-border-style fg=colour39   # bright blue for active pane

# --- Status bar ---
set -g status-right ""   # removes the clock/time

# --- Allow passthrough for Kitty graphics ---
set -g allow-passthrough on
set -ga update-environment TERM
set -ga update-environment TERM_PROGRAM


# --- Claude agent overview in choose-tree (prefix + s) ---
# @cis: is this pane a Claude agent? (process name is a version string, or
# the pane title carries Claude's status glyph)
set -g @cis '#{||:#{m/r:^[0-9]+\.[0-9]+\.[0-9]+$,#{pane_current_command}},#{m/r:^[✳◑◐◒◓✻✽*],#{pane_title}}}'
# @cdot: one green dot per Claude agent, nothing for other panes
set -g @cdot '#{?#{E:@cis},#[fg=green]●#[default],}'
# Rows: session -> a dot per agent in it; window -> just its name; pane -> the
# agent's task title. The leading pane dot becomes the state marker later.
set -g @ctree '#{?pane_format,#{E:@cdot} #{?#{E:@cis},#{pane_title},#{pane_current_command}},#{?window_format,#{window_name}#{window_flags},#{session_windows}w  #{W:#{P:#{E:@cdot}}}#{?session_attached, (attached),}}}'
# Panes: only show Claude agents. Sessions and windows always pass.
set -g @cfilter '#{?pane_format,#{E:@cis},1}'
bind s choose-tree -Z -F '#{E:@ctree}' -f '#{E:@cfilter}'

# List of plugins
set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'tmux-plugins/tmux-sensible'
set -g @plugin 'tmux-plugins/tmux-resurrect'
set -g @plugin 'tmux-plugins/tmux-continuum'

set -g @continuum-restore 'on'

# Other examples:
# set -g @plugin 'github_username/plugin_name'
# set -g @plugin 'github_username/plugin_name#branch'
# set -g @plugin 'git@github.com:user/plugin'
# set -g @plugin 'git@bitbucket.com:user/plugin'

# Initialize TMUX plugin manager (keep this line at the very bottom of tmux.conf)
run '~/.tmux/plugins/tpm/tpm'


